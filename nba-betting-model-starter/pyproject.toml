[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nba-betting-model-starter"
version = "0.1.0"
description = "Starter scaffold for an NBA betting model"
requires-python = ">=3.10"
